/** The running mix2 version. Bumped by each release commit together with
 * apps/tui/package.json and Cargo.toml; `version.test.ts` and the release
 * workflow both fail if the copies disagree. */
export const VERSION = '0.5.0';
